id: orchestrator.cache_hit_response_replay
agent: orchestrator
description: Cache-hit response replay (skipped; pipeline runs without cache_manager)
snapshot: home_default
language: en
context: {source: ha, area_id: living_room, device_id: satellite_living_room}
llm:
  classify: |
    general-agent (90%): cached question
  agents:
    general-agent: |
      Cached answer.
request:
  text: "cached query"
expected:
  routed_agent: general-agent
  service_calls: []
  speech_contains: ["cached"]
