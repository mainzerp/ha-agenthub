[pytest]
asyncio_mode = auto
timeout = 60
markers =
    integration: marks tests as integration tests (deselect with '-m "not integration"')
    real_scenarios: marks YAML-driven real-pipeline end-to-end scenarios
